<!doctype html>
<html lang="ru">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover,user-scalable=no"/>
<title>NNVPN — Ссылка недействительна</title>
<meta name="color-scheme" content="dark only"/>
<link rel="icon" href="data:,">
<link rel="preload" href="/fonts/Nunito/Nunito-Variable.woff2" as="font" type="font/woff2" crossorigin>
<link href="/fonts/nunito.css" rel="stylesheet">
<link href="/fonts/common.css" rel="stylesheet">
<script src="/fonts/telegram-web-app.js"></script>

<style>
:root{
  --bg-start:#0f0c29;
  --bg-mid:#24243e;
  --bg-end:#302b63;

  --panel-bg:rgba(28,30,51,.68);
  --panel-bg-strong:rgba(255,255,255,.07);
  --stroke:rgba(125,125,150,.30);

  --highlight-primary:#00f2fe;
  --highlight-secondary:#4facfe;
  --danger:#ff6b8a;
  --warning:#ffd166;

  --text-primary:#f0f4f8;
  --text-secondary:#a8b2d1;
  --text-muted:#7f8baa;

  --sat:env(safe-area-inset-top);
  --sab:env(safe-area-inset-bottom);
}

*{box-sizing:border-box}

html,body{
  height:100%;
  margin:0;
}

body{
  min-height:100%;
  display:grid;
  place-items:center;
  padding:calc(18px + var(--sat)) 16px calc(18px + var(--sab));
  font-family:'Nunito',system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;
  color:var(--text-primary);
  background:
    radial-gradient(circle at 20% 15%, rgba(79,172,254,.24), transparent 34%),
    radial-gradient(circle at 85% 80%, rgba(0,242,254,.18), transparent 36%),
    linear-gradient(-45deg,var(--bg-start),var(--bg-mid),var(--bg-end));
  background-size:120% 120%, 120% 120%, 400% 400%;
  overflow:hidden;
}

body::before{
  content:"";
  position:fixed;
  inset:-80px;
  background:
    radial-gradient(circle, rgba(255,255,255,.08) 0 1px, transparent 1.5px);
  background-size:26px 26px;
  opacity:.18;
  pointer-events:none;
  mask-image:linear-gradient(to bottom, transparent, #000 18%, #000 82%, transparent);
}

.wrap{
  width:min(520px,100%);
  position:relative;
  z-index:1;
}

.card{
  width:100%;
  text-align:center;
  background:var(--panel-bg);
  border:1px solid var(--stroke);
  border-radius:28px;
  box-shadow:
    0 24px 70px rgba(0,0,0,.38),
    inset 0 1px 0 rgba(255,255,255,.08);
  -webkit-backdrop-filter:blur(22px);
  backdrop-filter:blur(22px);
  padding:34px 22px 26px;
  animation:fadeIn .45s ease-out both;
}

.badge{
  display:inline-flex;
  align-items:center;
  gap:8px;
  padding:8px 13px;
  border-radius:999px;
  color:#ffe9ef;
  background:rgba(255,107,138,.13);
  border:1px solid rgba(255,107,138,.28);
  font-size:.9rem;
  font-weight:800;
  margin-bottom:18px;
}

.icon{
  width:82px;
  height:82px;
  margin:0 auto 18px;
  display:grid;
  place-items:center;
  border-radius:26px;
  background:
    linear-gradient(135deg, rgba(255,107,138,.24), rgba(255,209,102,.12)),
    rgba(255,255,255,.05);
  border:1px solid rgba(255,255,255,.12);
  box-shadow:0 16px 34px rgba(255,107,138,.14);
  font-size:2.6rem;
}

.title{
  font-size:clamp(1.45rem, 4.7vw, 2rem);
  line-height:1.15;
  font-weight:900;
  margin:0 0 12px;
  letter-spacing:-.02em;
}

.text{
  margin:0 auto;
  max-width:430px;
  color:var(--text-secondary);
  font-size:1.02rem;
  line-height:1.55;
}

.hint{
  margin:22px auto 0;
  padding:15px 16px;
  border-radius:20px;
  background:var(--panel-bg-strong);
  border:1px solid rgba(255,255,255,.10);
  color:var(--text-primary);
  font-size:.98rem;
  line-height:1.45;
}

.steps{
  margin:18px 0 0;
  padding:0;
  list-style:none;
  display:grid;
  gap:10px;
  text-align:left;
}

.steps li{
  display:flex;
  gap:10px;
  align-items:flex-start;
  padding:12px 13px;
  border-radius:18px;
  background:rgba(255,255,255,.045);
  border:1px solid rgba(255,255,255,.075);
  color:var(--text-secondary);
  line-height:1.35;
}

.num{
  flex:0 0 auto;
  width:24px;
  height:24px;
  border-radius:50%;
  display:grid;
  place-items:center;
  color:#061520;
  font-size:.82rem;
  font-weight:900;
  background:linear-gradient(90deg,var(--highlight-secondary),var(--highlight-primary));
}

.footer{
  margin-top:20px;
  color:var(--text-muted);
  font-size:.88rem;
}

.code{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  margin-right:6px;
  padding:2px 7px;
  border-radius:999px;
  color:#ffdce5;
  background:rgba(255,107,138,.12);
  border:1px solid rgba(255,107,138,.22);
  font-weight:900;
}

@keyframes fadeIn{
  from{opacity:0; transform:translateY(10px) scale(.97);}
  to{opacity:1; transform:translateY(0) scale(1);}
}

@media (max-width:380px){
  .card{padding:28px 16px 22px; border-radius:24px;}
  .icon{width:74px; height:74px; border-radius:23px;}
  .steps li{font-size:.94rem;}
}
</style>
</head>

<body>
  <main class="wrap">
    <section class="card">
      <div class="badge">Ссылка больше не активна</div>

      <div class="icon">🔗</div>

      <h1 class="title">Ссылка недействительна</h1>

      <p class="text">
        Эта ссылка для подключения больше не поддерживается или уже устарела.
        Получите новую актуальную ссылку прямо в Telegram-боте.
      </p>

      <div class="hint">
        Нажмите в боте кнопку <b>«Добавить устройство»</b> ещё раз — мы сразу выдадим новую рабочую ссылку.
      </div>

      <ul class="steps">
        <li>
          <span class="num">1</span>
          <span>Вернитесь в Telegram-бота.</span>
        </li>
        <li>
          <span class="num">2</span>
          <span>Нажмите <b>«Добавить устройство»</b> повторно.</span>
        </li>
        <li>
          <span class="num">3</span>
          <span>Используйте новую ссылку для подключения.</span>
        </li>
      </ul>

      <div class="footer">
        <span class="code">502</span>
        Если проблема повторяется — напишите в поддержку.
      </div>
    </section>
  </main>
</body>
</html>